using Matrix3D.Structures;
using System;
using System.Collections.Generic;
using System.Linq;
using OpenTK.Mathematics;
using System.Text;
using System.Threading.Tasks;

namespace Matrix3D.Rendering
{
    static class Sphere
    {
        /*private static (Vertex[], uint[]) GenerateVertices(int latitudes, int longitudes, int radius)
        {
            Vertex[] vertices = new Vertex[latitudes * longitudes];

            for (int i = 0; i < latitudes; i++)
            {
                for (int j = 0 ; j < longitudes - 1; j++)
                {
                    int index = (i * latitudes) + longitudes;
                    float x = (float)(Math.Sin(Math.PI * i / latitudes) * Math.Cos(2 * Math.PI * j / longitudes));
                    float y = (float)(Math.Sin(Math.PI * i / latitudes) * Math.Sin(2 * Math.PI * j / longitudes));
                    float z = (float)(Math.Cos(Math.PI * i / latitudes));

                }
            }
        }*/
    }
}
